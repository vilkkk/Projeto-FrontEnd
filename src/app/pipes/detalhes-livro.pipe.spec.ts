import { DetalhesLivroPipe } from './detalhes-livro.pipe';

describe('DetalhesLivroPipe', () => {
  it('create an instance', () => {
    const pipe = new DetalhesLivroPipe();
    expect(pipe).toBeTruthy();
  });
});
